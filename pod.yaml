---
 apiVersion: v1
 kind: Pod
 metadata:
   name: nodejs
   labels:
     app: web
 spec:
   containers:
     - name: nodejs
       image: nodejs
       imagePullPolicy: Never
       ports:
         - containerPort: 3000